from setuptools import setup, find_packages

setup(
    name='ycx_complex_numbers',
    version='0.1',
    description='A simple Python library for calculating representing complex numbers.',
    author='Graham Bevan [M0YCX]',
    packages=find_packages(),
)